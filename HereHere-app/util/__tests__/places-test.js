import { buildPlaceObject, getPlaceData } from '../places';

const placeObject = {
  placeId: '123',
  address: '123 6th street',
  placeName: 'place',
  coords: {
    latitude: '-75.0000',
    longitude: '45.0000'
  }
};

describe('Places utility helper tests', () => {

  it('builds a place object correctly', () => {

  });


});

describe('Google Places API tests', () => {

});